{
  "name": "shawnyeager-og-images",
  "version": "1.0.0",
  "description": "Open Graph image generation for shawnyeager.com",
  "type": "module",
  "scripts": {
    "generate-essays": "node scripts/generate-essays-json.js",
    "generate-og": "node scripts/generate-og-images.js",
    "build": "npm run generate-essays && npm run generate-og && hugo"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "glob": "^10.3.10",
    "gray-matter": "^4.0.3",
    "puppeteer": "^24.33.1",
    "satori": "^0.10.14",
    "serve-handler": "^6.1.6",
    "sharp": "^0.33.2"
  },
  "devDependencies": {
    "@netlify/functions": "^5.1.0"
  }
}
